<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mini-Airbnb</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <div class="container">
      <h1>Mini-Airbnb</h1>
      <p>Trouvez votre hébergement idéal pour votre prochain séjour</p>
    </div>
  </header>

  <main class="container">
    <h2>Nos logements disponibles</h2>
    
    <div class="logements-grid">
      <% logements.forEach(logement => { %>
        <div class="logement-card">
          <img src="/images/<%= logement.images[0] %>" alt="<%= logement.titre %>" class="logement-image"> <!-- Affichage de l'image -->
          <div class="logement-info">
            <h3><%= logement.titre %></h3>
            <p class="logement-emplacement"><%= logement.emplacement %></p>
            <p class="logement-capacite"><%= logement.capacite %> voyageurs • <%= logement.chambres %> chambre<%= logement.chambres > 1 ? 's' : '' %></p>
            <p class="logement-prix"><%= logement.prix %>€ / nuit</p>
            <a href="/logement/<%= logement.id %>" class="btn">Voir ce logement</a>
          </div>
        </div>
      <% }); %>
    </div>

    <!-- Pagination -->
    <div class="pagination">
      <% if (currentPage > 1) { %>
        <a href="/?page=<%= currentPage - 1 %>">Précédent</a>
      <% } %>

      <span>Page <%= currentPage %> sur <%= totalPages %></span>

      <% if (currentPage < totalPages) { %>
        <a href="/?page=<%= currentPage + 1 %>">Suivant</a>
      <% } %>
    </div>
  </main>

  <footer>
    <div class="container">
      <p>&copy; 2025 Mini-Airbnb - Projet éducatif</p>
    </div>
  </footer>
</body>
</html>
